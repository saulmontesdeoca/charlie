import { StandoutDirective } from './standout.directive';

describe('StandoutDirective', () => {
  it('should create an instance', () => {
    const directive = new StandoutDirective();
    expect(directive).toBeTruthy();
  });
});
